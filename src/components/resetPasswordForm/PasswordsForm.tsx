import { Box } from "@mui/material";
import { IPasswordsFormProps } from "./TypesResetForm";

const PasswordsForm = ({ cbHandleSavePassword }: IPasswordsFormProps) => {
  return <Box>PasswordsForm</Box>;
};

export default PasswordsForm;
